import React from 'react';

const CartHeader = () => {
    return ( 
        <nav className = "navbar navbar-dark bg-primary">
            <a className = "navbar-brand" href = "http://www.google.com" > Shopping Cart </a>  
        </nav>
    )
}

export default CartHeader;